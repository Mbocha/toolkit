import { FormControl } from "@angular/forms";

export interface User {
    fullName: any;
    organ: string;
    expert: string;
    phoneNum: string;
    email: any;
    imgurl: string;
    role: string;
  }